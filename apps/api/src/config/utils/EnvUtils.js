/**
 * Утилиты для работы с переменными окружения
 *
 * Модуль зарезервирован для будущих утилит работы с environment variables.
 * В настоящее время все необходимые операции выполняются напрямую через process.env
 * в конфигурационных классах с использованием Zod валидации.
 */
